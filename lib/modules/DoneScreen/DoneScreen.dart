import 'package:flutter/cupertino.dart';

class DoneScreen extends StatelessWidget {
  const DoneScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child : Text('Done Task')));
  }
}
